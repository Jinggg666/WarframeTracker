# Warframe Pricing · 战甲价格

Live market data for Prime Warframe sets and parts from warframe.market.

The rendering logic lives in `public/app.js` (the `loadGrid` / `renderDetail` functions); the backend lives in `server.js`. Files in this folder are reserved for any per-section static assets (custom thumbnails, build notes, etc).
